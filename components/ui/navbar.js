import { Box, Flex, Button } from '../ui/'

const Navbar = () => {
    return (
        <Flex h='5em' w='100%' bgColor='blue' alignItems='center'>
            <Button>Home</Button>
        </Flex>
    );
};

export default Navbar;